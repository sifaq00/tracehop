{
  "name": "tracehop-monorepo",
  "version": "1.0.0",
  "description": "TraceHop - Real-time Multi-chain Wallet Intelligence Layer",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @tracehop/web build",
    "dev": "pnpm --filter @tracehop/web dev",
    "db:migrate": "pnpm --filter @tracehop/db db:migrate",
    "db:seed": "pnpm --filter @tracehop/db db:seed",
    "start": "pnpm --filter @tracehop/web start",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "ts-node": "^10.9.2",
    "@types/node": "^20.11.0"
  }
}